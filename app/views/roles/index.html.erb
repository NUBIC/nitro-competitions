<h1>Listing roles for <%= @sponsor.program_title %></h1>

<table>
  <tr>
  <th>Name</th>
  </tr>

<% @roles.each do |role| %>
  <tr>
  <td><%= role.name %></td>
    <td><%= link_to 'Show', sponsor_role_url(:sponsor_id=>@sponsor.id, :id=>role.id) %></td>
  </tr>
<% end %>
</table>

<br />
